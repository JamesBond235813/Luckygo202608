import { useNavigate } from 'react-router-dom';
import Card from '../components/ui/Card';

export default function Me() {
  const navigate = useNavigate();
  return (
    <div className="min-h-screen bg-surface dark:bg-dark-surface p-4 pb-24">
      <h1 className="font-bold text-lg mb-4">Me</h1>
      <Card className="p-4 space-y-2">
        <button type="button" className="text-ghana-green font-semibold" onClick={() => navigate('/settings')}>
          Settings
        </button>
      </Card>
    </div>
  );
}
