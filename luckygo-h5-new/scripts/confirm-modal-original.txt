                {confirmOpen && (
                    <motion.div className="fixed inset-0 z-[65] flex items-center justify-center px-6 bg-black/50 backdrop-blur-sm">
                        <div className="w-full max-w-sm bg-white dark:bg-dark-card rounded-3xl p-6 shadow-2xl transition-colors">
                            <h3 className="text-lg font-black text-gray-900 dark:text-slate-100 mb-2">{t('productConfirmTitle')}</h3>
                            <p className="text-sm text-gray-500 dark:text-slate-400 mb-4 leading-relaxed">{t('productConfirmHint')}</p>
                            <motion.div className="space-y-2 mb-6 text-sm">
                                <div className="flex justify-between rounded-xl bg-gray-50 dark:bg-slate-800 p-3">
                                    <span className="text-gray-500 dark:text-slate-400">{t('productConfirmProduct')}</span>
                                    <span className="font-bold text-gray-900 dark:text-slate-100 text-right max-w-[60%] line-clamp-2">{product.title}</span>
                                </div>
                                <div className="flex justify-between rounded-xl bg-gray-50 dark:bg-slate-800 p-3">
                                    <span className="text-gray-500 dark:text-slate-400">{t('productConfirmShares')}</span>
                                    <span className="font-bold text-gray-900 dark:text-slate-100">{normalizedShares}</span>
                                </div>
                                <div className="flex justify-between rounded-xl bg-gray-50 dark:bg-slate-800 p-3">
                                    <span className="text-gray-500 dark:text-slate-400">{t('productConfirmTotal')}</span>
                                    <span className="font-black text-ghana-red text-base">{formatCurrency(orderTotal)}</span>
                                </div>
                            </motion.div>
                            <div className="flex gap-3">
                                <button
                                    type="button"
                                    onClick={() => setConfirmOpen(false)}
                                    disabled={isSnatching}
                                    className="flex-1 h-12 rounded-2xl border border-gray-200 dark:border-slate-700 text-gray-700 dark:text-slate-200 font-bold text-sm active:scale-95 transition-all disabled:opacity-60"
                                >
                                    {t('commonCancel')}
                                </button>
                                <button
                                    type="button"
                                    onClick={confirmPlaceOrder}
                                    disabled={isSnatching}
                                    className="flex-1 h-12 rounded-2xl bg-primary text-ghana-green-dark font-black text-sm active:scale-95 transition-all disabled:opacity-70"
                                >
                                    {t('productConfirmSubmit')}
                                </button>
                            </div>
                        </div>
                    </motion.div>
                )}

                {showSuccess && (