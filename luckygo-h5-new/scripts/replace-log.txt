
=== line 1947 len 1060
                {showSuccess && (
                    <div className="fixed inset-0 z-[70] flex items-center justify-center px-6 bg-black/60 backdrop-blur-md">
                        <div className="relative w-full max-w-sm bg-white dark:bg-dark-card rounded-3xl p-8 flex flex-col items-center text-center shadow-2xl transition-colors">
                            <button
                                type="button"
                                onClick={() => setShowSuccess(false)}
                                className="absolute top-4 right-4 size-9 rounded-full bg-gray-100 dark:bg-slate-800 flex items-center justify-center active:scale-95 transition-transform"
                                aria-label={t('commonClose')}
                            >
                                <span className="material-symbols-outlined text-xl text-gray-600 dark:text-slate-300">close</span>
                            </button>
                            <div className="size-20 rounded-full bg-ghana-green/10 flex items-center justify-center mb-6">

=== line 1949 len 1067
                {showSuccess && (
                    <div className="fixed inset-0 z-[70] flex items-center justify-center px-6 bg-black/60 backdrop-blur-md">
                        <motion.div className="relative w-full max-w-sm bg-white dark:bg-dark-card rounded-3xl p-8 flex flex-col items-center text-center shadow-2xl transition-colors">
                            <button
                                type="button"
                                onClick={() => setShowSuccess(false)}
                                className="absolute top-4 right-4 size-9 rounded-full bg-gray-100 dark:bg-slate-800 flex items-center justify-center active:scale-95 transition-transform"
                                aria-label={t('commonClose')}
                            >
                                <span className="material-symbols-outlined text-xl text-gray-600 dark:text-slate-300">close</span>
                            </button>
                            <div className="size-20 rounded-full bg-ghana-green/10 flex items-center justify-center mb-6">
