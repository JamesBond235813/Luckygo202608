# Home 商品上下排列修正

## 本轮完成内容

- 修正 `luckygo-h5-new/src/pages/Home.tsx` 的商品布局，取消两个商品左右并排的双列布局。
- Home 继续只展示带 `tag` 的推荐商品。
- 推荐商品只有 1 个时，只渲染 1 条商品行。
- 推荐商品有 2 个时，渲染上下 2 条商品行，每条只显示 1 个完整商品卡片。
- 推荐商品超过 2 个时，按交错顺序分配到上下两条横向滑轨；每条滑轨每次只显示 1 个完整商品卡片，手指左右滑动可以查看该行的下一个商品。
- 保留现有滚动播报、Banner、商品详情跳转、参与按钮和开奖倒计时刷新逻辑。

## 验证结果

- `npm run lint` 已通过。
- `npm run build` 已通过。
- 本地当前有 2 个推荐商品，手机尺寸 `390 x 844` 下验证为上下两行。
- 两张商品卡宽度均为页面可用宽度 `374px`，不再左右并排。
- 构建仅保留既有的前端包体积提示，不影响本轮功能。

## 本地隔离说明

- 本轮只修改本地 H5 页面源码、构建输出和本轮总结文件。
- 前端仍连接本地 `http://localhost:3000/api`，后端仍连接本地 MySQL `127.0.0.1:3306` 的 `luckygo_local` 数据库。
- 未连接、修改或部署任何云端服务、数据库、配置或文件。
- 没有修改数据库表结构。
