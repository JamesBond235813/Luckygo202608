# LuckyGo 本地临时通用验证码作业总结

日期：2026-08-27

## 配置结果

- 开发环境通用验证码：`888888`
- 配置位置：`luckygo-server/.env`
- `H5_OTP_DEV_BYPASS_ENABLED=true`，仅本地开发环境启用。
- `SMS_GHANA_ENABLED=false`，不会调用真实短信网关。
- `H5_OTP_RETURN_CODE=true`，请求验证码时本地接口会返回开发验证码提示，便于联调。

## 使用方式

在 H5 登录弹窗中输入任意合法的 9 位加纳本地手机号，点击 `Send code`，验证码输入 `888888`，勾选年龄确认后点击 `Log in` 即可进入登录/注册流程。

## 验证结果

- 已用测试手机号调用 `/api/auth/otp/request`，接口成功返回开发验证码 `888888`。
- 未修改数据库表、字段或既有生产业务逻辑。
- `.env` 已被 git 忽略，不会提交临时凭据。

