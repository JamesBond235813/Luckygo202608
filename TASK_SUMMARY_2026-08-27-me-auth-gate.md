# LuckyGo H5 我的页登录门禁作业总结

日期：2026-08-27

## 本次目标

调整 H5 `/me` 页面：移除未登录访客提示区域，并确保所有需要真实身份的操作先进入统一注册/登录流程；已登录用户继续使用原有页面跳转和业务逻辑。

## 完成内容

- 移除未登录页面顶部的 `Log in to start winning`、账户同步提示文案和 `Log in now` 按钮。
- 未登录点击 `Beans & Tasks` 时，先通过统一 `promptLogin` 登录门禁，不再直接进入 `/rewards`。
- 未登录点击余额卡片、余额显示切换、充值、兑换、中奖记录、参与记录、地址、修改手机号、设置、通知、邀请奖励等身份相关入口时，均使用统一登录门禁。
- 未修改后端业务逻辑，也未删除数据库表或字段。

## 涉及文件

- `luckygo-h5-new/src/pages/Me.tsx`

## 验证结果

- `npm run build`：通过。
- `npm run lint`：通过。
- H5 服务已确认监听 `0.0.0.0:2088`，访问 `/me` 返回 HTTP 200。
- 登录门禁仍沿用现有 `/login` 页面及返回路径机制，避免重复实现注册、OTP 和年龄确认流程。

