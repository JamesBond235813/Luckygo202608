# Home 紧凑商品卡调整

## 本轮完成内容

- 调整了 `luckygo-h5-new/src/components/home/HomeProductCards.tsx`。
- 为 `HomeTrendingCard` 增加仅供 Home 使用的 `compact` 紧凑布局选项，不影响分类页等继续使用标准大卡的页面。
- Home 推荐商品卡从纵向大图卡改为横向紧凑卡：左侧商品图片，右侧标题、剩余数量、进度和参与按钮。
- 紧凑商品卡固定高度为 `142px`，Home 页面已启用该样式。

## 验证结果

- 本地当前 2 个推荐商品保持上下排列。
- 在 `390 x 844` 手机尺寸下：第一张卡片位于 `y=375` 至 `y=517`，第二张位于 `y=533` 至 `y=675`。
- 页面主体在 `y=679` 结束，滚动公告、Banner 和两张商品卡均完整显示在首屏内。
- `npm run lint` 已通过。
- `npm run build` 已通过。
- 构建仅保留既有的前端包体积提示，不影响本轮功能。

## 本地隔离说明

- 本轮只修改本地 H5 商品卡源码、Home 页面引用、构建输出和本轮总结文件。
- 前端继续连接本地 `http://localhost:3000/api`，后端继续连接本地 MySQL `127.0.0.1:3306` 的 `luckygo_local` 数据库。
- 未连接、修改或部署任何云端服务、数据库、配置或文件。
- 没有修改数据库表结构。
