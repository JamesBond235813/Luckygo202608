# Home 推荐商品布局调整

## 本轮完成内容

- 调整了 `luckygo-h5-new/src/pages/Home.tsx` 的 Home 商品展示逻辑。
- Home 现在只展示推荐商品：沿用现有数据约定，将带有 `tag` 的在售、已满员待开奖或开奖中商品作为推荐商品。
- 移除了原先按 `tag` 拆分的 Hot Picks 与 Trending 两个独立商品区，避免首页重复或展示非推荐商品。
- 推荐商品数量大于 2 时，商品按索引交错分到上下两条横向滑轨；每条滑轨可左右滑动查看多个商品。
- 推荐商品恰好为 2 时，改为两列大卡布局。滚动播报、Banner 和两张商品卡会填满手机首屏。
- 移除了 Home 页面底部重复的成年提示条；成年验证仍由现有进入页面时的验证弹窗负责，避免两商品首页产生额外页面高度。

## 当前结果

- 本地接口 `http://127.0.0.1:3000/api/campaigns` 当前返回 2 条进行中的带标签活动，均被 Home 作为推荐商品展示。
- 在 `390 x 844` 手机尺寸下验证，两张商品卡共同占据 Banner 下方至首屏底部的空间。
- `npm run lint` 已通过。
- `npm run build` 已通过。
- 构建仅保留既有的前端包体积提示，不影响本轮功能。

## 本地范围与约束

- 本轮修改了本地源码 `/Users/jackbond/Desktop/luckygo/luckygo-h5-new/src/pages/Home.tsx`，并因构建同步刷新了本地 H5 `dist` 输出及本总结文件。
- 前端配置继续指向本地 `http://localhost:3000/api`，后端配置继续使用本地 MySQL `127.0.0.1:3306` 的 `luckygo_local` 数据库。
- 未连接、修改或部署任何云端服务、数据库、配置或文件。
- 本轮没有修改数据库表结构。

## 后续维护说明

- 当前项目没有独立的“推荐”字段，推荐状态由商品 `tag` 是否有值决定。
- 若后续需要将标签文案和是否首页推荐分开管理，应新增独立的推荐字段和管理端开关；按照项目约束，数据库调整只能新增，不能删减现有表或字段。
