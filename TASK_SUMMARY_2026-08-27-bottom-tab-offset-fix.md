# 作业总结

## 问题

分类页快速刷新后，底部 Tab 偶发出现水平位置偏移。

## 原因

底部导航的 4 个按钮使用 `width: 100%` 放入 flex 容器，实际布局依赖浏览器对超宽 flex 子项进行自动压缩；刷新期间字体和尺寸变化可能导致压缩结果不稳定。

## 修复

- 将底部 Tab 改为明确的 `flex: 1` 等分布局。
- 增加 `min-width: 0`，避免标签文本撑开按钮。
- 保留原有路由、选中态和点击逻辑。

## 验证

- 在 `430x932` 移动视口连续刷新后，4 个 Tab 中心点稳定为等距布局。
- H5 `npm run build` 通过。
- H5 `npm run lint` 通过。
