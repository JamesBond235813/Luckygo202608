# LuckyGo H5 登录浮层与交互重排作业总结

日期：2026-08-27

## 本次目标

登录触发时保持当前页面不变，在当前页面上方打开注册/登录弹窗，并按移动端操作习惯重排 OTP、勾选和按钮。

## 完成内容

- `promptLogin` 改为派发全局登录弹窗事件，不再把当前页面导航到 `/login`。
- `App` 增加全局登录弹窗承载层，登录成功后回到触发操作的原页面。
- 登录弹窗增加遮罩、右上角关闭按钮和底部取消按钮，关闭时保持原页面和返回路径。
- 验证码改为单输入框，与 Get OTP 按钮同一行；支持手动输入、`one-time-code` 系统自动填充及浏览器 Web OTP 自动读取短信。
- 年龄确认和隐私/条款同意内容移动到取消、登录按钮上方。
- 取消和登录按钮改为同一行并排布局。
- 邀请奖励、空数据登录入口和设置页登录入口统一改用当前页面登录浮层。
- 保留原有手机号校验、OTP 请求、邀请推荐码、年龄确认和登录接口逻辑。

## 验证结果

- `npm run build`：通过。
- `npm run lint`：通过。
- H5 已重新启动：`http://192.168.1.150:2088`。
- 未修改后端逻辑及数据库表结构。

