# 本轮任务总结：统一验证码登录与自动注册

## 任务目标

将 H5 的登录和注册合并为一个手机号验证码入口：

- 已注册手机号：验证码正确后直接登录。
- 未注册手机号：验证码正确、完成年龄确认后自动创建用户并登录。
- 不迁移现有技术栈，不触碰云端。

## 已完成修改

### 后端

- 扩展 `POST /api/auth/otp/verify`，支持 `inviteCode` 和 `ageConfirmed`。
- 统一验证码登录逻辑：已有用户登录，新手机号自动建档。
- 保留原密码登录、密码注册和独立 OTP 注册接口作为兼容接口，但 H5 主流程不再调用它们。
- 保留邀请码奖励逻辑，新用户仍可通过邀请码获得对应奖励。
- 保留年龄确认校验，新用户未确认年龄时不能完成自动注册。

### H5 前端

- 登录页不再显示“登录/注册”切换。
- 移除手机号密码、确认密码表单。
- 新入口包含：手机号、验证码、可选邀请码、年龄确认。
- 增加验证码发送、60 秒重发倒计时、验证码输入校验和验证中状态。
- 已注册和新注册均通过同一个 `ApiService.loginWithOtp` 请求。
- 邀请分享链接从 `/register` 改为 `/login`，邀请码仍会自动带入并锁定。
- 更新帮助中心和邀请说明，去除主登录流程中的密码和分离注册描述。

## 验证结果

- H5 lint 通过：0 错误、0 警告。
- H5 构建通过。
- 后端构建通过。
- `GET http://127.0.0.1:5173/login` 返回 HTTP 200。
- `POST /api/auth/otp/verify` 使用已有手机号和开发验证码返回 `isNewUser: false` 并签发 token。
- 新手机号首次验证返回 `isNewUser: true` 并签发 token。
- 同一新手机号再次验证返回 `isNewUser: false`，确认自动注册后可正常登录。
- 本地服务仍连接 `127.0.0.1:3306/luckygo_local`，未执行云端操作。

## 当前本地服务

- H5：`http://127.0.0.1:5173/login`
- 后端：`http://127.0.0.1:3000`
- 本地开发验证码：使用当前本地配置的开发验证码机制。

## 备注

- 本轮测试使用了本机数据库中的一个新测试手机号，以验证自动注册分支；没有写入云端数据库。
- 设置页中的“修改密码”和换绑手机号密码校验属于账户管理兼容功能，本轮没有删除；后续如要彻底取消密码体系，需要单独改为短信验证码确认流程。
