# 本轮任务总结：验证码表单参考布局调整

## 任务目标

根据提供的参考图重新设计统一手机号验证码登录页的表单区域，并满足以下要求：

- 保留现有 EBA Promo 页头，不改动品牌页头设计。
- 保留邀请码输入框。
- 保留成年确认勾选声明。
- 将隐私政策和服务条款设计为底部默认已勾选的确认项。

## 已完成修改

- 恢复并保留原有绿色 EBA Promo 品牌页头、Logo、标题和提示标签。
- 仅重做页头下方的登录表单布局：
  - 加纳国旗、`+233` 区号、号码长度计数。
  - 六个独立验证码输入格。当前后端验证码规则为六位，因此没有改为参考图中的四位。
  - 验证码发送、自动聚焦、倒计时重发、退格回退和数字粘贴输入支持。
  - 保留可选邀请码；邀请链接携带的邀请码仍自动填入且不可修改。
  - 保留独立的成年确认勾选声明。
  - 底部隐私政策与服务条款使用默认勾选状态，仅作为已阅读同意提示，不需要用户手动操作。
- 页面主按钮在未发送验证码时为“发送验证码”，发送后变为“验证并继续”。
- 新增中英文界面文案。

## 验证结果

- H5 lint 通过：0 错误、0 警告。
- H5 构建通过。
- 本地 H5 登录页可访问：`http://127.0.0.1:5173/login`，HTTP 200。
- 未修改后端统一验证码登录、自动注册、数据库或云端配置。

## 当前效果

登录页保持原有品牌页头，手机号和验证码输入区域采用参考图的高可读性、独立验证码格和底部操作形式，同时保留 LuckyGo 的统一验证码登录、邀请码和成年确认业务逻辑。
